import Area from "../../utils/data/Area";
import Node from "../../utils/data/Node";
import { Coordinate } from "../../utils/interfaces";


const NodePrompt = ({ currentArea, coordinates, closeMenu, addNode }: { currentArea: Area; coordinates: Coordinate; closeMenu: () => void; addNode: (node: Node) => void; }): JSX.Element => {
  return (
    <div>

    </div>
  );

};

export default NodePrompt;